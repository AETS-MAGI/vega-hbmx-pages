# vega-hbmx-pages sample code dependencies (ROCm)
#
# Important:
# - PyTorch ROCm wheels are typically available for Python 3.10/3.11.
# - If your venv is Python 3.14, create a Python 3.11 venv first.
#
# Example:
#   python3.11 -m venv .venv311
#   source .venv311/bin/activate
#   python -m pip install --upgrade pip
#   python -m pip install -r requirements.txt

--index-url https://download.pytorch.org/whl/rocm6.3
torch
numpy
